
spring:
  application:
    name: mlb-content
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        extension-configs:
          - data-id: redis-config-dev.yaml
            refresh: true
          - data-id: content-mysql-dev.yaml
            refresh: true